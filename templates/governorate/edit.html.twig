{% extends 'base-admin.html.twig' %}

{% block title %}Edit Governorate{% endblock %}

{% block body %}
    <a class="btn btn-secondary mb-3" href="{{ path('governorate_index') }}">
        <i class="fa fa-arrow-circle-left"></i>back to list</a>

    <div class="card p-3">
        <h1>Edit Governorate</h1>
        {{ include('governorate/_delete_form.html.twig') }}
        {{ include('governorate/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    
{% endblock %}
